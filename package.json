{
  "name": "jova",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "npx eslint . --ext js,ts,jsx,ts,json --fix"
  },
  "lint-staged": {
    "src/**/*.{ts,tsx,js}": [
      "npx prettier --write",
      "npx eslint --fix"
    ],
    "*.{js,json}": [
      "npx prettier --write",
      "npx eslint --fix"
    ]
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "dependencies": {
    "next": "10.0.6",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  },
  "devDependencies": {
    "eslint": "^7.20.0",
    "eslint-plugin-prettier": "^3.3.1",
    "husky": "^5.0.9",
    "prettier": "^2.2.1"
  }
}
